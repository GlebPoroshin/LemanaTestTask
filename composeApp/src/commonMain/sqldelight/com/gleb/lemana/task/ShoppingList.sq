CREATE TABLE shopping_list (
    product_id INTEGER NOT NULL PRIMARY KEY
);

getAllProductIds:
SELECT product_id FROM shopping_list;

insertProductId:
INSERT OR IGNORE INTO shopping_list(product_id) VALUES (?);

deleteProductId:
DELETE FROM shopping_list WHERE product_id = ?;
